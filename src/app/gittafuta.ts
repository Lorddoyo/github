export class Gittafuta {
  constructor(public username: string, public name: string,public avatar:any, public bio: any, public repositories: any, public followers: any, public following: any, public link: any,public date:Date){
    this.username=username; this.name=name, this.bio=bio;this.repositories=repositories;
  }
}
